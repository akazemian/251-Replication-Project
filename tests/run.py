
# def main(model_name, benchmark, metric, layer):

    # load model config and get identifier
    # config = load_yaml('config')[model_name]
    # define model iden 
    
    # define model tokenizer
    
    # instantiate model
    # instantiate benchmark

    # load model stimuli
    # run model to get activations 
    # run benchmark on metric to get scores
    # get pearson correlation
    
